{
  "name": "documentacion-tecnica-firplak-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "web"
  ],
  "scripts": {
    "build": "npm run build --prefix web",
    "install": "PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true npm install --prefix web"
  }
}
