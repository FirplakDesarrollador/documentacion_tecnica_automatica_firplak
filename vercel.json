{
  "version": 2,
  "buildCommand": "npm run build --prefix web",
  "installCommand": "PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true npm install --prefix web",
  "outputDirectory": "web/.next",
  "framework": "nextjs"
}
